# 软著补正递交说明（流水号 2026R11L1402160）

软件全称（补正后）：**去旅聚户外活动报名与社交App软件** [简称：去旅聚] V1.0  
软件形态：**移动终端 App**（与微信小程序、网页版为不同登记事项，本件仅登记 App）  
著作权人：枣庄禾跃科技有限公司  

> **重要**：申请表「软件全称」须与鉴别材料页眉逐字一致。旧称「去旅聚户外活动报名与社交应用软件」或「…微信小程序软件」请改为上表全称。

## 补正项 1：申请确认签章页

**须在版权中心网上系统处理，无法由本仓库 PDF 代替。**

1. 登录 [商标/版权网上服务系统](https://sbj.cnipa.gov.cn) → 找到本申请 → 补正。  
2. 下载**申请确认签章页**范本。  
3. 将页面上**划线部分全文手抄**（一字不漏）。  
4. 加盖申请人公章，法定代表人签字（按范本要求）。  
5. 扫描为 PDF 上传。

## 预审意见对照（程序 + 文档）

### 意见 1：代码、文档页眉软件名称与申请表不一致

**处理：**

1. 在版权中心系统核对申请表 **软件全称** 是否为（逐字）：  
   `去旅聚户外活动报名与社交App软件`  
   **简称**：`去旅聚`。**版本号**：`V1.0`。  
   程序鉴别、文档鉴别、签章页、申请表四者须完全一致。

2. 页眉统一为：  
   `去旅聚户外活动报名与社交App软件  V1.0`

3. 源程序摘录已通过 `sanitize_soft_registration_outputs.py` 将旧称「旅聚」统一为「去旅聚」（与页眉、申请表一致）；前端源码亦已同步更名。

4. 重新生成并递交：  
   - `program_identification_material.pdf`（60 页源程序）  
   - `documentation_identification_full.pdf`（用户手册；若界面截图仍为旧文案，请重跑 `capture_h5_screenshots.py` 后重建文档 PDF）

```bash
cd wander_meet
.venv-soft-reg/bin/python scripts/rebuild_soft_registration.py
```

### 意见 2：文档体现为 App、建议全称以 App 结尾

**申请人说明（可写入补正答复）：**

1. 本件登记软件为**移动终端 App**，软件全称已调整为「**去旅聚户外活动报名与社交App软件**」，与申请表及鉴别材料页眉一致。

2. 文档鉴别材料已按图文结合编写，含登录、主界面、各功能操作步骤及 App 手机端完整界面截图。

3. 软件分类按 **App / 移动应用软件** 填报（以系统选项为准）。

---

## 补正项 2：用户操作手册（文档鉴别材料 · 图文结合）

针对「文字过多、应图文结合、须含登录/主界面/操作步骤及完整手机端截图」：

- 已重写 `manual_content_zh.txt`：按**登录 → 主界面 → 浏览报名 → 发布 → 消息 → 个人中心**编排，每节**短文字 + 一张完整手机端截图**。
- 截图已加**手机外框**（`capture_h5_screenshots.py`），PDF 内放大显示。
- 重新生成 `documentation_identification_full.pdf` 后递交。

---

## 补正项 2（原）：用户操作手册（文档鉴别材料）

**递交文件（二选一，建议优先完整版）：**

| 文件 | 页数 | 说明 |
|------|------|------|
| `documentation_identification_full.pdf` | 约 65 页 | **推荐补正递交**：含全部章节与界面截图 |
| `documentation_identification_submit.pdf` | 60 页 | 仅当前 30 + 后 30 页；若系统限制单文件 60 页再用 |

- 已更新软件名称为「去旅聚」。  
- 正文已**嵌入界面截图**（`screenshots/`，来自客户端界面真机或 H5 渲染，非 PIL 示意图）。

重新生成（含截图 + 手册 PDF）：

```bash
cd wander_meet
python3 -m venv .venv-soft-reg && .venv-soft-reg/bin/pip install -r requirements-soft-reg-pdf.txt
.venv-soft-reg/bin/playwright install chromium   # 首次需下载浏览器
.venv-soft-reg/bin/python scripts/capture_h5_screenshots.py
.venv-soft-reg/bin/python scripts/build_doc_identification_pdf.py
```

一键（含源程序 60 页 + 截图 + 手册）：

```bash
.venv-soft-reg/bin/python scripts/rebuild_soft_registration.py
```

若已用**真机**截屏，将 PNG 按 `scripts/build_doc_identification_pdf.py` 中 `CAPTION_TO_FILE` 对应文件名覆盖 `doc/soft_registration/screenshots/` 后，仅重跑 `build_doc_identification_pdf.py` 即可。

## 补正项 3：源程序（程序鉴别材料）

**递交文件：** `program_identification_material.pdf`（**共 60 页**）

- 第 1–30 页：连续源程序前段（后端 + 前端 `travel-together/src`）。  
- 第 31–60 页：连续源程序后段。  
- 每页 **50 行**；已脱敏（数据库连接、密钥等）。  

## 配置与脚本

| 文件 | 说明 |
|------|------|
| `scripts/soft_reg_config.py` | 软件全称、版本、著作权人 |
| `scripts/rebuild_soft_registration.py` | 一键生成全部材料 |
| `doc/soft_registration/manual_content_zh.txt` | 手册源稿 |

## 30 日时限

收到补正通知书之日起 **30 日内** 提交，逾期视为撤回。
